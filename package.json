{
  "name": "katachi",
  "version": "0.1.0",
  "description": "Small Electron app for playing against KataGo's human SL network",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check renderer.js"
  },
  "devDependencies": {
    "electron": "^42.3.0"
  }
}
